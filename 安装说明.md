# maths 安装包

在另一台 Mac 解压后，双击 `install.command`，或将解压目录交给 Codex 并说“请安装这里的 maths 技能”。需要 Python 3.10+；安装器优先使用 Codex 本机运行时。

完整步骤、API 配置和错题迁移方法见 [安装与使用](maths/references/setup.md)。Windows 可运行 `install.ps1` 或 `py -3 install.py`。

本包包含个人初始学习设置，不包含真实错题或 API 密钥。安装不会覆盖已有学生档案。验证范围见同目录的 `验证记录.md`。
